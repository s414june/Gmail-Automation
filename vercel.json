{
	"version": 2,
	"builds": [
		{
			"src": "server.ts",
			"use": "@vercel/node"
		}
	],
	"routes": [
		{
			"src": "/api/(.*)",
			"dest": "server.js"
		}
	],
	"buildCommand": "npm install && npm run build",
	"startCommand": "npm start"
}
