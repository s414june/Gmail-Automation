{
	"version": 2,
	"installCommand": "npm install",
	"buildCommand": "npm run build",
	"outputDirectory": "./dist",
	"functions": {
		"server.js": {
			"runtime": "@vercel/bash"
		}
	},
	"rewrites": [
		{
			"source": "/api/(.*)",
			"destination": "/dist/server.js"
		}
	]
}
